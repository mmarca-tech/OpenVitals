<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        The `namespace` is declared in android/build.gradle. Bluetooth
        `<uses-permission>` entries (BLUETOOTH_SCAN, BLUETOOTH_CONNECT,
        BLUETOOTH_ADVERTISE) live in the HOST APP manifest — they must be
        declared by the installable app — so this library manifest is empty.
    -->
</manifest>

import 'package:flutter/material.dart';

import '../../ui/components/placeholder_screen.dart';

/// Daily-readiness overview pushed over the shell (`/daily_readiness`).
// TODO(phase5): replace with the real daily-readiness overview.
class DailyReadinessScreen extends StatelessWidget {
  const DailyReadinessScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const PlaceholderScreen(title: 'Daily readiness');
}

import 'package:flutter/material.dart';

import '../../ui/components/placeholder_screen.dart';

/// Cardio-load detail pushed over the shell (`/activity/cardio_load`).
// TODO(phase5): replace with the real cardio-load detail.
class CardioLoadDetailScreen extends StatelessWidget {
  const CardioLoadDetailScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const PlaceholderScreen(title: 'Cardio load');
}

import 'package:flutter/material.dart';

import '../../ui/components/placeholder_screen.dart';

/// Settings nav-suite branch body (rendered inside the adaptive scaffold).
// TODO(phase5): replace with the real settings entry list.
class SettingsScreen extends StatelessWidget {
  const SettingsScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const PlaceholderBody(title: 'Settings');
}

**SettingsListItem** — a tappable settings row (its own card): leading glyph, title + supporting text, trailing chevron. Stack with 8px gaps for the Settings screen.

```jsx
<SettingsListItem icon="tune" title="Display" supportingText="Language, units, and theme" onClick={() => {}} />
```

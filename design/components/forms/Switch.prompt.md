**Switch** — Material 3 settings toggle. Pass `label` for a full-width settings row (label left, switch right); omit it for a bare switch.

```jsx
<Switch checked={on} onChange={setOn} label="Hydration reminders" />
```

**MetricLineChart** — the detail-screen trend chart (Body Energy timeline, heart-rate over time). Smooth accent line over a soft area fill with Y gridlines.

```jsx
<MetricLineChart data={[81,82,83,83,84]} yTicks={[0,50,100]}
  accentColor="var(--ov-metric-workout)" height={180} />
```

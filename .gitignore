# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
debug-screenshots/
.claude/
# Keystore / signing
*.jks
*.keystore
*.p12
*.pfx
*.pkcs12
keystore.properties
.woodpecker/*.env
.woodpecker/secrets/
.woodpecker/tmp/

# Local release helpers
.env
.env.local

# Stray crash dumps
Error in OpenVitals *.txt

# Gradle/Kotlin leftovers. The Kotlin app lived at the repo root, so a stray Gradle
# invocation here (or an old checkout) leaves these behind; Flutter's own Android
# build stays under android/ and /build/.
/.gradle/
/.kotlin/
/local.properties
/app/build/
/captures/

# Design-system reference bundle (React/TS UI kit). Kept on disk as a design
# reference, but not versioned in the app repo -- the Kotlin repo gitignored it too.
docs/proposals/OpenVitals Design System/

**TimeRangeSelector** — the Day/Week/Month/Year segmented control on period detail screens. Selected segment uses `primary-container`; controlled via `value`/`onChange`.

```jsx
<TimeRangeSelector value={range} onChange={setRange} />
```

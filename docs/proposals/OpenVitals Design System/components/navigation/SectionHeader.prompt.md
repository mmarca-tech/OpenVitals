**SectionHeader** — a quiet section label ("Activities", "Support") in `on-surface-variant`. Pass `trailing` (a Material Symbols glyph name like `chevron_right`) for a "see all" affordance.

```jsx
<SectionHeader text="Activities" trailing="chevron_right" onTrailingClick={() => {}} />
```

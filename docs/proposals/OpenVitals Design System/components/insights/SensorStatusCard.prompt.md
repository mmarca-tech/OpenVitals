**SensorStatusCard** — the dashboard sensor/battery status row. The icon + battery accent shift with the lowest sensor battery (>40 primary, ≤40 green, ≤20 red).

```jsx
<SensorStatusCard batteryPercent={64} activeCount={2} connectedCount={1} onClick={() => {}} />
```

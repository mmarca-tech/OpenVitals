**Select** — Material 3 outlined dropdown (units, language, theme mode, favorite activity). Options can be strings or `{value,label}`.

```jsx
<Select label="Units" value={units} onChange={setUnits} options={['Metric', 'Imperial']} />
```

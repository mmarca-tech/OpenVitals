**RadioGroup** — a Material 3 radio list, controlled. Options can be strings or `{value,label}`.

```jsx
<RadioGroup value={theme} onChange={setTheme}
  options={['System', 'Light', 'Dark', 'AMOLED']} />
```

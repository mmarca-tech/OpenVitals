OpenVitals is native Kotlin again, with everything the 2.x line added; your data and settings import automatically on first launch. Smartwatches sync through Health Connect (e.g. Gadgetbridge), with the original source shown. Sync health data to another phone over Bluetooth, offline. Import CSV files, record without GPS, follow-system units with per-quantity overrides, sturdier reminders, and fixes for sleep double-counting and slow yearly views.

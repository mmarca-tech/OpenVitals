OpenVitals no longer contains any Google Play Services code. The location plugin pulled in Google's proprietary location library, and F-Droid will not build an app that carries it. The app never used it: route recording has always asked Android's own location manager for satellite fixes, because Google's fused provider mixes in network and wifi positions that are not the GPS readings a route is made of. Nothing about recording changes.
